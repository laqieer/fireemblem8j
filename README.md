# Fire Emblem: Seima no Kouseki (聖魔の光石)

[![CI](https://github.com/laqieer/fireemblem8j/actions/workflows/ci.yml/badge.svg)](https://github.com/laqieer/fireemblem8j/actions/workflows/ci.yml)
[![Code](https://decomp.dev/laqieer/fireemblem8j/jp.svg?mode=shield&measure=matched_code_percent&label=Code)](https://decomp.dev/laqieer/fireemblem8j/jp)
[![Data](https://decomp.dev/laqieer/fireemblem8j/jp.svg?mode=shield&measure=matched_data_percent&label=Data)](https://decomp.dev/laqieer/fireemblem8j/jp)
[![Functions](https://img.shields.io/endpoint?url=https%3A%2F%2Fprogress.deco.mp%2Fdata%2Ffireemblem8j%2Fjp%2Fdefault%2F%3Fmode%3Dshield%26measure%3Dfunctions&label=Functions)](https://laqieer.github.io/fe-decomp-portal/)
[![FE Decomp Portal](https://img.shields.io/badge/progress%20charts-FE%20Decomp%20Portal-2ea44f)](https://laqieer.github.io/fe-decomp-portal/)

A decompilation / disassembly of **Fire Emblem: The Sacred Stones — Japanese version**
(`Fire Emblem - Seima no Kouseki`, game code `BE8J`).

It builds the following ROM:
* `fireemblem8.gba` `sha1: 7da0456035366aa18414faa79d8fe7649f03c1ed`

This is the JP counterpart of the (essentially complete) US decomp
[laqieer/fireemblem8u](https://github.com/laqieer/fireemblem8u). The two ROMs are
compiled from the same Intelligent Systems source by the same compiler, so the
bulk of the work is **re-linking the US C source against the JP ROM's data
layout** and decompiling the comparatively small set of region-specific code and
data (text, fonts, menus, save versioning).

## Goal

**Build the ROM byte-for-byte from committed source with `baserom.gba` removed.**
A real decompilation reproduces the ROM from matching C (`src/*.c`),
descriptive/data assembly (`asm/*.s`), and **extracted, descriptively-named
source assets** (PNG/`.pal` graphics, charmap-encoded text, C struct tables,
music) — compiled by an asset toolchain. The original ROM, `baserom.gba`, is
**only** the verification target of `make compare`; it is **never a build input**.
Delete `baserom.gba` and `make` must still emit the byte-identical ROM (this is
what `fireemblem8u` and `pokeemerald` already do). See
[`docs/decomp-completion-standard.md`](docs/decomp-completion-standard.md) for the
full standard and the honest audit behind the numbers below.

This is **not** "`asm/baserom.s` has zero incbins" (a cosmetic count, gameable by
relocating incbins into other files). The single number that matters is **build
self-containment**: remove `baserom.gba` and see if `make` still builds. Run
`python3 scripts/check_selfcontained.py` for it, and
`python3 scripts/calcprogress.py` for all four axes.

## Status — honest scorecard

FE8J is an **in-progress** decompilation, but it has reached the foundational
milestone: **✅ the ROM builds byte-perfect from committed source with `baserom.gba`
removed.** `mv baserom.gba away && make` emits the byte-identical JP ROM (sha1
`7da0456…`) — the self-contained build **PASSES** (the blocking CI gate). The four
real axes (target: **100%** each):

| Axis | Today | Meaning |
|---|---|---|
| **Build self-containment** | **100%** ✅ | the ROM builds from committed source with `baserom.gba` absent — **0 `.incbin "baserom.gba"`** directives. The only ungameable number, and it's met. |
| **Matching-C functions** | **~91.5%** (7,863 / 8,528) | functions whose bytes come from compiling `src/*.c`. **D242 (multi-TU stranded batch, +10):** prepscreen (GetUnitFromPrepList, RegisterPrepUnitList, PrepGet/SetUnitAmount), bmbattle (InitBattleUnitWithoutBonuses, CheckBattleUnitStatCaps, BattleUnitTargetCheckCanCounter), bmdifficulty (DungeonRecordUi_InitText), savedraw (SaveDraw_InitParticles), scene (StartTalk) — all clean via auto-extracted fe8u includes. 10×diff 0. **D241 (fontgrp.c stranded batch, +15):** 15 of the 25 fontgrp.c stranded fns carve clean (InitText, InitTextDb, PutText, PutBlankText, Text_Skip, Text_GetChrOffset, IsTextPrinting, InitSpriteText, GetTextDrawDest, GetSpriteTextDrawDest, DrawSpecialCharGlyph_old, ClearSmallStringBuffer, PrintEmptyStringToDBG, PrintNumberToDBG, InitTextInitInfo); 10 CFAIL on TU-local statics/helpers (deferred). 15×diff 0. **D240 (stranded-section vein generalized, hardware +6):** the D239 individually-sectioned-stranded-incbin carve generalizes to ANY TU's `stranded_func_<TU>.tsv` (regular agbcc). Carved all 6 `hardware.c` stranded fns (SetMainUpdateRoutine, CopyTilemapScreen, GetBackgroundFromBufferPointer, ClearTileRigistry, RegisterDataMove, RegisterFillTile). ~90 more stranded fns remain across fontgrp(25)/_unmapped(51)/prepscreen/bmbattle/etc. 6×diff 0. **D239 (m4a stranded-section batch via old_agbcc, +17):** the 17 region-same m4a functions held as descriptive incbins in `asm/stranded_m4a.s` (each individually sectioned) all carve to matching-C under old_agbcc: MPlayContinue, MPlayFadeOut, MPlayStart, m4aMPlayFadeIn/FadeOutTemporarily/ImmInit/TempoControl/ModDepthSet/LFOSpeedSet, m4aSoundMode/VSyncOff/VSyncOn, FadeOutBody, TrkVolPitSet, CgbOscOff, CgbModVol (static-inline CgbPan), ply_xwave (TU-local READ_XCMD_BYTE macro). Rewrote each stranded tsv line to `src/<Fn>.o(.text)` + pruned the incbin block. 17×diff 0. **D238 (CgbSound via old_agbcc, +1):** `CgbSound` (0x080D5A28, 1100B) — the m4a CGB (GameBoy PSG) channel-processing function (319 lines of C); old_agbcc reproduces it byte-exact over all 1100 bytes. Only needed a forward decl of `CgbModVol` (JP-undeclared). diff 0. Completes the named m4a vein (ClearModM, 5×song-num, m4aSoundInit, MPlayExtender, CgbSound = +8 this session). **D237 (MPlayExtender via old_agbcc + gbadisasm range-split, +1):** `MPlayExtender` (0x080D512C, 280B) — the m4a CGB-channel/jump-table extender; old_agbcc codegen exact. Needed `gMaxLines = 0` bound as an absolute linker symbol (fe8u `gMaxLines = 0;`), and the gbadisasm range (0D512C–0D5248, 284B) had bundled a trailing 4-byte BIOS `svc #0x2A; bx lr` wrapper (JP 0x080D5244) into MPlayExtender — split it into `asm/sub_80D5244.s` so MPlayExtender carves at its true 280B (else subsequent functions shift −4). diff 0. **D236 (m4aSoundInit via old_agbcc + raw-addr RAM globals, +1):** `m4aSoundInit` (0x080D4E70, 120B) — old_agbcc reproduces the codegen byte-exact (reloc-excluded diff 0), but the m4a RAM globals (gSoundInfo, gCgbChans, gMPlayMemAccArea, SoundMainRAM_Buffer) are auto-placed by src/m4a.c at the WRONG IWRAM addresses (0x03000150 vs JP 0x03005400 etc.), so their literal-pool words mismatched under full make compare (D215 trap). Fixed by `#define`-ing the four to their real JP runtime addresses (raw-addr). diff 0. **D235 (m4a song-num family via old_agbcc, +5):** `m4aSongNumStart`/`Stop`/`Continue`/`StartOrChange`/`StartOrContinue` (0x080D4EF4–0x080D4FF4) — the gMPlayTable/gSongTable mplay-dispatch wrappers; direct fe8u m4a.c ports, all matched once routed through the D234 `old_agbcc` per-target override. diff 0 ×5. **D234 (m4a old_agbcc toolchain lever, +1):** `ClearModM` (0x080D5FE0, 36B) — the m4a sound-engine TUs were built with the OLD GBA SDK agbcc; their codegen shape (prologue, dead `movs r2,#0` reg-init, `adds r1,r0,#0` base copy) differs from the regular agbcc and only byte-matches under `old_agbcc`. Added a per-target Makefile override `src/ClearModM.o: CC1 := $(CC1_OLD)` (mirroring fe8u's `src/m4a.o`). diff 0. Opens the m4a still-asm functions as a fresh vein. **D233 (extern-inline accessors, bmitem family, +1):** `IsItemEffectiveAgainst` (0x08016994, 156B) — JP inlines `GetItemEffectiveness` (`gItemData[idx].pEffectiveness`@0x10), `GetItemAttributes` (@8) and `GetItemData` as `extern inline` (D102); the goto-based flier-negation check + the 5-slot attribute OR loop port verbatim. `ItemEffectiveness_Flier`/`FlierAndMonsters` resolve by name (src/data/data_itemuse). diff 0. **D232 (6-field MAC loop direct port, +1):** `ApplyAffinitySupportBonuses` (0x080284E4, 96B) — `bonuses->bonusX += level * GetAffinityBonuses(affinity)->bonusX` unrolled over the 6 SupportBonuses fields (offset 1..6); byte-identical to JP. GetAffinityBonuses (0x080284C0) already bound; forward-declared (JP bmreliance.h omits it). diff 0. **D231 (ProcCmd handler direct port + TU-local enum, +1):** `ProcCmd_SET_BIT4` (0x080032C8, 24B) — `proc->proc_flags |= PROC_FLAG_UNK2; proc->proc_scrCur++; return TRUE;`; PROC_FLAG_UNK2=(1<<2) is a TU-local enum in fe8u proc.c (JP-undeclared), defined locally. diff 0. **D230 (2-call wrapper + static→global callee, +1):** `WfxFlames_VSync` (0x08030930, 16B) — `WfxFlamesUpdateGradient(); WfxFlamesUpdateParticles();`. Both callees were already carved (bmio_0803082C.o / bmio_WfxFlamesUpdateParticles.o) but `WfxFlamesUpdateGradient` was `static` (internal linkage); made it global (byte-neutral — linkage doesn't change code bytes) so the cross-TU `bl` resolves. diff 0. **D229 (extern-inline accessors + Text_GetColor bind, +1):** `DrawItemMenuLineNoColor` (sub_8016750, 132B) — JP inlines three `bmitem.c` accessors (`GetItemName` wrapping arg-less `StrInsertTact()`, `GetItemUses`=`ITEM_USES`, `GetItemIconId`=`item ? gItemData[idx].iconId : -1`) as `extern inline` (D102), AND bound `Text_GetColor` @0x08003D94 (a 4-byte sibling of `Text_SetColor`, missing from baseline_syms). diff 0. **D228 (IDA cascade-root: JP cursor + extra string-draw, +1):** `ClassChgMenuOnDrawCore` (sub_80D2938, 136B) — JP uses `Text_SetCursor(text, 0)` not fe8u's `8`, and adds an extra `Text_DrawString(text, (char*)0x081F6D7C)` (a direct ROM string) before the `str` draw. diff 42→0. **D227 (JP OAM-tile scale const + raw-addr sprite, +1):** `PutClassNameIntroLetter` (sub_80B762C, 376B) — the two `PutSpriteExt` tile args use `charId * 4` in JP, not fe8u's `charId * 2` (a 2-byte `lsls #1`→`#2` diff ×2); plus raw-addr the unbound TU-static sprite `sSprite_Opinfo_0` @0x08AAFCAC. diff 0. **D226b/c/d (Sio family DECL + JP-msgid, +3):** `DrawLinkArenaTeamListMenu` (sub_8043744, msgid MSG_76B→0x58 + extern gUnk_Sio_14[][15]), `SioTeamList_EraseTeam` (sub_8043574) + `LoadLinkArenaTeamList` (sub_8043344, 244B) — same as D226: TU-local `struct LinkArenaTeamEnt` + extern gLinkArenaTeamList + extern `gSioTeamListConfigLut` + msgid MSG_0CC→0x61. **D226 (local-struct DECL + JP-msgid, +1):** `SioTeamList_SwapTeams` (sub_8043644, 256B) — defined the local `struct LinkArenaTeamEnt` (20B, JP-undeclared) + `MULTIARENA_LIST_SWAP`=4 enum + msgid MSG_0CC→0x61; gLinkArenaTeamList bound at 0x0203DB78. **D225 (JP-msgid const, +1):** `SioTeamList_StartEraseTeamSubMenu` (sub_8044340, 132B) — "Erase"/"Back" msgids MSG_140/MSG_141 (0x140/0x141) → JP 0x7C1/0x7C3 (a different message region; US small values compile inline, JP large ones pool-load). **D224 (embedded-table + JP-msgid, +1):** `SioMenu_GetItemHelpText` (sub_8047970, 96B) — local `linkMenuMsgLut[10]` msgid table (JP = US − 0x75: MSG_735→0x6C0, MSG_736-73B→0x6C1-0x6C6) embedded FAR at 0x0DEE88; gap-split `frontier_df4_misc_lo.gap4` around the .rodata. The JP msgid table family (sio/item display strings) is a rich embedded-table vein. **D223 (extern-inline + embedded-table gap-split, +2):** `GetItemDisplayRangeString` (sub_8016A68) + `GetItemDisplayRankString` (sub_8016B3C, adjacent table @0x0DC630; `GetItemRequiredExp` returns `weaponRank`@0x1C) — extern-inline `GetItemEncodedRange`/`GetItemData` + the local `rangeTextIdLookup[10]` msgid table is region-different (JP 0x4AE–0x4B7 = US 0x522–0x52B − 0x74) and placed FAR at 0x0DC608; carved by changing the values to JP and gap-splitting the `frontier_df4_misc_lo.gap0b2` residual INCBIN around the .rodata (D121 embedded-table pattern). **D222 (JP msgid const, +1):** `PrepScreen_StartUnitSwap` (sub_8033B20, 124B) — single JP difference: the "Reorder your units" message id is 0x7F0 (2032) not `MSG_872`/0x872 → diff 42→0. **D221 (cast-hoist + decl-reorder, +2):** `BgAffinScaling` (sub_80B2924, 72B) + `BgAffinAnchoring` (sub_80B296C, 88B) — the `s16 sy/sx` params double-extended (zero-then-sign, +8B); cast-hoist `int syy=(s16)sy; int sxx=(s16)sx` collapses it AND declaring them BEFORE the `affin=NULL` local matches the JP sign-ext-then-NULL order → diff 0. **D220 (cast-hoist double-extension, +1):** `BrownTextBox_SetBlendFlag` (sub_8011B08, 36B) — the `s8 doBlend` param was zero-extended then re-sign-extended (8 redundant bytes; US 44→JP 36); `int db = (s8)doBlend` collapses it to a single entry sign-ext → diff 0. Refines the cast-hoist: works even WITHOUT a held-across-call (a redundant double-extension is enough). **D219 (IDA cascade-root + msgid + raw-addr, +1):** `DrawLinkArenaLoadingScreen` (sub_8046728, 112B) — JP OMITS the `Text_SetCursor(GetStringTextCenteredPos(96, ...))` centering line (diff 41→8), the "Now Loading" msgid is 0x4D not `MSG_77D`/0x77D (JP uses an immediate, fe8u a pool load), and `gUnk_Sio_6` is unbound EWRAM → raw-addr `(struct Text *)0x0203DA74`. **D218 (DECL_ONLY + cast-hoist, +1):** `StartTalkFaceMove` (sub_800795C, 80B) — declared `gProcScr_TalkFaceMove` + cast-hoist the `s8 isSwap` param (stored to proc->unk6A after Proc_Start, held across the call) via `int sw = (s8)isSwap` → diff 0. **D217b/c (extern-inline accessor family, +2):** `CanUnitUseStaffNow` (sub_80165A8) + `CanUnitUseWeaponNow` (sub_80164F8) — same D102 extern-inline GetItemAttributes/GetItemData fix → diff 0. **D217 (extern-inline accessor, D102, +1):** `GetUnitItemHealAmount` (sub_8016D60, 116B) — JP inlines `GetItemIndex`/`GetItemAttributes`/`GetItemData` (table lookups) where fe8u's JP headers declare them as plain functions. Provided all three as `extern inline` (GNU C89 = inline-only, no out-of-line dup) → the switch + IA_STAFF check inline to JP's `gItemData[item&0xFF].attributes` form → diff 0. **D216 (IDA cascade-root, JP-omitted call, +1):** `PrepItemUse_PostPromotion` (sub_809EF04, 48B) — JP omits the `PrepSetLatestCharId(proc->unit->pCharacterData->number)` call (size 60→48). Same omitted-call vein as the Exec* staff family. **D215 (DECL_ONLY struct + JP layout consts, +1):** `ClearGMapPIPanel` (sub_80C3618, 188B) — defined the local `struct Unknown8A3E448` (4×s8) + declared `gWorldmapPlayerInterface_0`, then the JP GMap player-info panel is narrower (`TileMap_FillRect` width 13→12 ×8) AND its right-column offset is shifted (`+0x011`→`+0x012`, `+0x211`→`+0x212`). The width is an immediate (sadiff-visible) but the offset lives in the literal-pool address (reloc — sadiff-BLIND); only full cold make compare caught it (D126). **D214 (DECL_ONLY + JP gBmMapRange-reuse, +1):** `AiFindClosestTerrainAdjacentPosition` (sub_803AEB4, 328B) — declared JP-bound `AiGetPositionRange`, then JP reuses `(s8)gBmMapRange[]` for the best-distance comparison where fe8u references the separate `gMapRangeSigned[]` symbol (one pool entry vs two). Full make compare confirms gBmMapRange is the correct reloc. **D213 (JP menu-def string-pointer, +2):** `WMMenu_OnGuideDraw` (sub_80C1338, 116B) — same fix as MapMenu_GuideCommandDraw (a D213-family member: any menu *Draw fn doing `Text_DrawString(text, GetStringFromIndex(...->def->nameMsgId))` → `*(const char **)...->def`). `MapMenu_GuideCommandDraw` (sub_8022644, 104B) — IDA showed JP passes `*menuItem->def` (a direct `char*` string pointer, the def's first field) to `Text_DrawString`, where fe8u does `GetStringFromIndex(menuItem->def->nameMsgId)`. JP menu-item defs store string pointers, not msgids — `*(const char **)menuItem->def` → diff 0. A reusable lever for the menu Draw family. **D212 (IDA JP-rewrite reconstruct + size-delta screen, +1):** `GameIntroHealthSafetyWaitButton` (sub_80BCC90, 96B) — the JP function is an ENTIRELY DIFFERENT (simpler) implementation than fe8u's palette-fade wait loop: `SetBlendConfig(1,8,16,0)`, `SetBlendTargetA(0,0,1,0,0)`, `SetBlendTargetB(1,1,1,1,1)`, `SetDispEnable(1,1,1,1,1)`, `Proc_Start(<ProcScr 0x08AC0EBC raw-addr>, proc)`. Reconstructed from IDA → diff 0. Found via a size-delta screen (US-compiled size ≫ JP size = JP omits/rewrites). **D211 (IDA cascade-root, JP-omitted tutorial block, +1):** `NewBattleForecast` (sub_80373E4, 104B) — IDA showed JP omits the `if (CheckBattleForecastTutorialEvent() == 1) { SetKeyStatus_IgnoreMask(... | A_BUTTON); }` block (the JP ROM has no battle-forecast tutorial). Removing it → diff 0. **D210 (IDA cascade-root, JP-omitted guard + raw-addr, +1):** `RepairSelectOnSelect` (sub_80298E4, 96B) — IDA showed JP omits the `if (GetPortraitData(GetUnitPortraitId(...))->img)` portrait guard, calling `StartFace`/`SetFaceBlinkControlById` unconditionally. Removing the guard → diff 0; then raw-addressed the unbound `gMenuInfo_RepairItems` at 0x085C5544 (`(const struct MenuDef *)`, read from the asm pool) to resolve the link. **D209 (IDA cascade-root, JP-omitted block, +1):** `MapAnim_DisplayDeathQuote` (sub_807CD3C, 96B) — IDA showed JP omits the entire `switch(pid){ case CHARACTER_EIRIKA/EPHRAIM: if(CheckFlag(EVFLAG_GAMEOVER)) ClearFlag(EVFLAG_GAMEOVER); }` block (goes straight from pid to `CheckBattleDefeatTalk`). Removing it → diff 0 (the JP map-anim death-quote path doesn't special-case the lords' gameover flag). **D208 (Exec* staff family — same JP-omitted call, +1):** `ExecFortify` (sub_802F08C, 120B) — same root as D207: JP omits `BattleInitItemEffectTarget(GetUnitFromCharId(GetPlayerLeaderPid()))`. The JP item-use staff effects systematically drop that fe8u line. **D207 (IDA cascade-root, JP-omitted call, +1):** `ExecLatona` (sub_802F238, 104B) — IDA Hex-Rays showed the JP Latona-staff effect OMITS the entire `BattleInitItemEffectTarget(GetUnitFromCharId(GetPlayerLeaderPid()))` line that fe8u has (JP goes straight from `BattleInitItemEffect` to `MakeTargetListForLatona`). Deleting that one line → diff 0 (a behavioral/gameplay difference between the JP and US Latona). **D206 (DECL_ONLY + cast-hoist, +1):** `StartSupportUnitSubScreen` (sub_80A69D4, 36B) — same pattern as D205: declared the JP-bound ProcScr `gProcScr_SupportUnitSubScreen` (`extern struct ProcCmd[]`) → diff 1 (entry sign-ext). The `s8 fromPrepScreen` param is stored to `proc->fromPrepScreen` AFTER `Proc_StartBlocking` (held across the call); cast-hoist `int fp = (s8)fromPrepScreen` forces the entry asr → diff 0. **D205 (DECL_ONLY + cast-hoist, +1):** `UpdateStatArrowSprites` (sub_8015BF0, 72B) — declared the two JP-bound-but-undeclared LUTs (`gSysUpArrowSpriteLut`/`gSysDownArrowSpriteLut`, `extern u16 *[]`) → diff 1 (a single entry sign-ext `lsrs→asrs`). The 3rd param `isDown` is held in r4 across the `GetGameClock` call then tested; JP sign-extends it at entry. `s8 isDown` alone didn't force it (boolean truth-test → agbcc zero-extends), but the **D155 cast-hoist `int d = (s8)isDown`** did (no proto change → zero caller risk). This refines the "boolean-param sign-ext dead-end": when the value is HELD ACROSS A CALL in a callee-saved reg, the cast-hoist works. **D204 (DECL_ONLY + cast-hoist + permuter-found callee-proto, +1):** `PutWMFaceOnBg` (sub_80BD194, 132B) — three independent JP differences stacked: (1) DECL_ONLY — `GetWMFaceTileXAndScroll` is JP-bound but only a `// ???` stub in headers (declared `int GetWMFaceTileXAndScroll(int xIn, int *xOut)`); (2) the 5th param `flip` is `s8` (JP sign-extends at entry, `asrs`) — forced via the D155 cast-hoist `int f = flip` (removes agbcc's zero-ext + redundant re-extension, −4B → the real 132B); (3) a residual r9↔sl reg-alloc swap (`offset` vs `flip` across the call) that no source reorder could force — the **decomp-permuter** (base score 20, found 0 in 9078 iters) revealed the root: the callee `PutFaceOnBackGround`'s 3rd param is `int` in JP, not fe8u's `s8`. The `s8` prototype made agbcc narrow the arg at the call, which pinned the swapped allocation; declaring it `int c` (zero blast-radius — PutFaceOnBackGround is still asm and PutWMFaceOnBg is its only src/ caller) flips it → diff 0. **D203 (dual-const SETUP carve, +1):** `SioBat_InitSetupScreen` (sub_8045D60, 324B) — IDA showed a structure-matching diff=37 cascading from TWO JP consts: `StartFace`'s face id is `FID_ANNA + 1` = 0x65 in JP (the JP face table inserts one entry) and the "Please wait" message id is 0x6D3 not `MSG_748`/0x748 (the JP id isn't `imm8<<shift`-expressible, so agbcc loads it from a pool word — that pool word is the +4 bytes). Substituting both → diff 0. Continues the D202 behavioral-const SETUP vein. **D202 (behavioral-const SETUP class, extra-call, +1):** `SioBat_InitSetupScreen` (sub_8045D60, 324B) — IDA showed a structure-matching diff=37 cascading from TWO JP consts: `StartFace`'s face id is `FID_ANNA + 1` = 0x65 in JP (the JP face table inserts one entry) and the "Please wait" message id is 0x6D3 not `MSG_748`/0x748 (the JP id isn't `imm8<<shift`-expressible, so agbcc loads it from a pool word — that pool word is the +4 bytes). Substituting both → diff 0. Continues the D202 behavioral-const SETUP vein. **D202 (behavioral-const SETUP class, extra-call, +1):** `AtMenu_ResetScreenEffect` (sub_8098574, 80B) — IDA showed JP calls an extra `EndAllProcChildren(proc)` at the start (the +4B; fe8u lacks it). Adding it → diff 0. A fresh sub-class (lever-switch from sign-ext to disp/blend SETUP functions where a single JP behavioral difference — extra call / different arg — cascades). **D199 (HP-bar sibling pair, +2):** `NewEfxHpBarLive` (sub_80536FC) + `NewEfxHpBarResire` (sub_80532DC) carved by the same D198 off_next cast-hoist (`int off_next = (s16)(off_this+1)`) + raw-addr their unbound ProcScrs (0x085E386C / 0x085E380C). The cascade-root SOP generalizes across a function family. **D198 (cast-hoist cascade-root, +1):** `NewEfxHpBar` (sub_8052FFC, 228B) — a 65-byte diff (incl. a `0x94` "reg-alloc" cluster) all CASCADED from one root: `off_next`'s s16 narrowing was zero-extended (`lsrs`) where JP sign-extends (`asrs`). `int off_next = (s16)(off_this + 1)` fixed it (and removed the redundant re-sign-extension, shrinking 232→228 = the real size, confirmed by the next symbol `EfxHpBar_DeclineToDeath` @ 0x530E0). Plus raw-addr the declared-but-unbound `ProcScr_efxHPBar` @ 0x085E37E4 (read the pool VALUE, not IDA's pool-address ref). Confirms D197: pervasive diffs cascade from a single root — decode the FIRST diff, not the cluster. **D197 (IDA-driven logic-different carve, +1):** `OpAnimApplyScrollPalettes` (sub_80CC1B0, 272B) byte-matched via IDA Hex-Rays MCP — the JP scroll-palette setup has a behavioral difference: its `else`-branch enables **BG1+BG2** (`SetDispEnable(0,1,1,0,0)`, DISPCNT `&0xE0|6`) where fe8u enables BG0 (`SetDispEnable(1,0,0,0,0)`). The apparent reg-alloc diffs (bg2cnt `movs r0,#2`→`r2`) were a *cascade* from that one arg change — fixed together. (The IDA technique also reconstructed `OpAnimFaceMontageBegin` to diff=12, blocked there by an agbcc loop-invariant-hoist-order dead-end — see `reference/nonmatching/`.) **D193 (cursor-const + JP-hardcoded-string-ptr, +1):** `DrawChapterStatusTextForUnit` (sub_8090474, 564B) byte-matched — region-same modulo a `Text_SetCursor` coord `179→177` and JP replacing `GetStringFromIndex(MSG_539)` with a direct ROM string pointer `(char *)0x081F5548` (decoded from the literal pool; the missing `bl GetStringFromIndex` shortened the fn by 4B, exactly resolving the gbadisasm-range vs real-size discrepancy — boundary confirmed by the next symbol `ChapterStatus_ShowAllLayers` @ 0x906A8). MSG_535 already matched JP (=0x535). **D192 (msgid-const-decode, +1):** `SoundRoom_InitText` (sub_80B4B48, 232B) byte-matched as a region-SAME function (size matched) modulo 3 JP constants read from the literal pool: `InitText` width `5→4`, and the two `GetStringFromIndex` msgids `0x5AA→0x535` / `0x5AE→0x748` (JP soundroom text-table indices; agbcc strength-reduces 0x748 to `movs#0xe9;lsls#3`). Needed the local `struct Unknown201F148` def (JP headers lack it). LESSON: don't over-fit — an extra JP pool word (0x020228A8 = `&gPaletteBuffer[0x1A0]`) was just the auto-resolving reloc, not a constant-fold; raw-addressing it BROKE the match (size 232→228). `InitDifficultySelectScreen` skipped (push-list `f0→70` = reg-alloc structural). **D191 (s8 flag cast-hoist + misbound-sym raw-addr, +1):** `StartLinkArenaPointsNumberMovers` (sub_8049EF4) — a genuine multi-bit `u8 flag` param zero-extended by agbcc but JP sign-extends; fixed with `int fl = (s8)flag` placed *as the first statement* (statement order matters — it must precede `count=0` to match JP's flag-narrowed-first prologue) routing all uses through `fl`. Plus `gUnk_Sio_1` is misbound in `jp_syms.s` to the sentinel `0x4645464E`; referenced it raw at its US-confirmed `0x02000C78` (the `sub_80498B8.c` convention). This round also confirmed the naming-sweep levers exhausted (fingerprint 0 hits, caller-fingerprint bogus) and 5 NEAR dead-ends (boolean sign-ext `ShopTryMoveHand`/`HbMoveCtrl_OnIdle`, reg-swap `ReadSramFast`/`WriteSramFast`, struct-field sign-ext `GmMuPrim_TrackMovementDelta`, mov-sl/r9-churn `StartSubSpell_efxIvaldiOBJ1`). **D190 (s16 cast-hoist + raw-addr ProcScr, +1):** `NewEfxRestWINH` (sub_805C5D8) — a genuine `s16` scroll-offset param zero-extended by agbcc (only feeds strh) forced to JP's sign-ext via `int bb = (s16)b` (D153; works because the value is negative-capable, unlike the D189 boolean dead-ends), plus a declared-but-unbound `ProcScr_EfxRestWINH` referenced raw at 0x085FF238. **D189 (DECL_ONLY + layout-coord const, +1):** `SoundRoomUi_Init` (sub_80B4144, 720B) byte-matched after declaring ~10 JP-undeclared soundroom siblings (the headers carry only `// ??? X(???)` stubs), referencing the two still-unbound graphics by raw ROM address (`Img/Pal_SoundRoomUiElements` = 0x08AAAC4C/0x08AAB440, deduced by pool-order elimination against the bound `*_PlayStatusSprites` siblings), and fixing one JP layout-coord const — the completion-% text draws at `TILEMAP_LOCATED(gBG0TilemapBuffer, 16, 6)` (index 0xd0) not `15,6` (0xcf), one tile right. NEAR/CFAIL screen reconfirmed the boolean-param sign-ext (`ShopTryMoveHand`, `HbMoveCtrl_OnIdle`) and reg-swap (`ReadSramFast_Core`/`WriteSramFast` — `REG_WAITCNT & ~3` r0↔r1) residuals as lsr↔asr/reg-alloc dead-ends (agbcc proves the 0/1 value non-negative → won't sign-extend). **D187/D188 (region-diff op-anim ending pair, +2):** `OpAnimEphraimExit` (sub_80CD2E0, 404B) + `OpAnimEirikaExit` (sub_80CCAC0, 400B) byte-matched by full disasm-driven reconstruction. Key insight: **JP swaps the two routes' ending layouts vs fe8u** — JP-Ephraim uses fe8u-**Eirika**'s constants `(8,0)`/`0x158`/`0x98`/`0x1BC`/`0xC0` + Reverse-first/Maybe-second + an extra `SetPrimaryHBlankHandler(NULL)` before `Proc_Break`; JP-Eirika uses fe8u-**Ephraim**'s `(0xE8,0x100)`/`0x170`/`0x88`/`0x180`/`0xC8` + normal Maybe-first/Reverse-second, no extra call. Both replace fe8u's single name-sprite with a JP 2-sprite pair (Ephraim Obj 0x08B3F1F0/0x08B3F204; Eirika Obj 0x08B3F1CE/0x08B3F1DC). The `TsaModifyFirstPal{Maybe,Reverse}` call *targets* (verified by decoding each `bl`: Maybe@0x0CB720, Reverse@0x0CB7D8) are the discriminator — JP-Ephraim's swapped order only surfaced under full cold `make compare` (the reloc-excluded range-diff hit 0 but a swapped `bl` between same-signature siblings is reloc-masked). The global Tsa labels stay load-bearing (FlyIn/FlyOut/opanim-main callers use the normal order and still match). **D155 (DECL_ONLY proc-init + int-promotion vein, +7):** the `screen_named_rd.py` CFAIL bucket (compile-fails only on a JP-bound-but-undeclared symbol) is a fresh matching-C lever — many are region-SAME proc-init / Proc_Find-dispatch functions that byte-match once the symbol is declared (`extern struct ProcCmd X[]`) or referenced by raw address when its US name is mis-bound (`Proc_Find((const struct ProcCmd*)0x085D4760)`). Combined with the **int-promotion** fix for an `s8`/`s16` param held across a call then stored to a wider field: JP sign-extends once at entry, agbcc zero-extends then re-extends after the call (the `(s8)` cast *restructures* reg-alloc) — promoting via a plain `int ps = param;` local collapses to the single entry `asr`. Carved StartSioWarpFx + StartSioWarpFxPartial (sio.h `u8 playStepSe`→`s8`), NewBMXFADE, MakeNew6CBMXFADE2, SioMain2_WaitEndAndRoute, StartGmapMuEntry1, SetUiCursorHandConfig, PointsNumberMover_Init, Menu_AutoHelpBox_OnLoop (JP inlines the US `inline` HasMenuChangedItem), StartGmapAutoMu_Type1. Also: SaveMenuDrawSubSelBox, ExecTrap (JP trap msgids 0x81B/0x81C), SMS_DisplayOne (int-promote s8 isBlend), TurnRecord_SetupText (JP msgids 0xE6/0xDF + InitText widths 4/9/3). Skipped region-diff: NewPopup_VerySimple (popup opcodes≠enum), Uidebug_PickRandomActiveCond2 (reg-alloc), ClearGMapPIPanel (const), StrInsertTact (branch-merge codegen-shape), PutWmDotSprite/GmapEffect_0 (bounds-check fold), EventShinningCursorAdvance/StartStoneShatterAnim (s16-cast scheduling — permuter-class). **D153 (rename→screen→sign-ext-fix pipeline, +14):** the rename-unlocked `screen_named_rd.py` NEAR bucket is a deep vein — the dominant residual is a 1-byte `0e→16`/`0c→14` s8/s16 sign-extension (our agbcc zero-extends, JP sign-extends), fixed by `int x` + an early `(s8)`/`(s16)` cast (local-only for stored values; +header proto change for params). Carved StartSallyCirProc, StartEventEarthQuake, NewEfxFlash{HPBar,Unit,UnitEffectEnd}, UnitList_DrawPageHeader, LoadUnit_MoveToPosition, EkrPrepareBanimfx, GmapRmBorder1_PutSprite2, StartScriptBattleAnim, GmMu_0. CAUTION: a proto change in a shared header needs a stale-local-decl sweep across `src/` + a FOREGROUND clean build (one stale `s8` decl in unitlistscreen_0809404C.c silently broke clean builds until caught). ~24 NEAR + 6 CARVE + 63 CFAIL(decl-only) remain. the byte-neutral `rename_aliased` passes gave region-different `sub_` their US names, which unlocked `scripts/screen_named_rd.py` (compile each US body, byte-diff vs JP excluding relocs, bucket CARVE/NEAR/FAR). The dominant NEAR class is a 1-byte `0e→16`/`0c→14` = an s8/s16 **sign-extension** (`lsrs→asrs`): the US declares the value u8/u16/s8/s16 and our agbcc zero-extends, but JP sign-extends. Fix = force `asr` with `int x` + an early `(s8)`/`(s16)` cast (local-only when it is a stored value; +header proto change when it is a param, clean-build-verified). Carved StartSallyCirProc, StartEventEarthQuake, NewEfxFlashHPBar/Unit/UnitEffectEnd, UnitList_DrawPageHeader, LoadUnit_MoveToPosition; ~35 NEAR + 6 CARVE(data-bind) remain. **D151 (decomp-permuter cracks reg-alloc dead-ends):** the reg-alloc/scheduling near-misses hand-decomp can't force (e.g. an r4↔r5 swap on two s16 params) yield to `tools/decomp-permuter` — import the JP `glabel` asm + the near-match C, run `permuter.py … --stop-on-zero` (via the harness background, since the wrapper's detach trips the wall-clock timeout). It found a byte-exact `GmapRm_SetPosition` (result-temp + `do{}while(0)` grouping that forces x→r4,y→r5) in 126 iterations. **D150:** newlib stubs carve too (`_unlink`→`return -1`, `__errno`→`return *(int**)addr`; note calcprogress's `internal()` filter doesn't COUNT `__`-prefixed symbols even though they're genuinely decompiled). Also confirmed (4th time) the **data axis is genuinely 100%** — the 1451 `asm/dat_*_ref.s` residual files are NOT compiled; the manifest attributes all 13,937,984 data bytes to `src/data/*/dat_*_ref.o` typed-INCBIN objects (`data_asm=0`); self-contain 100% is the ungameable proof. And the **named scorecard is at its effective ceiling**: renaming aliased `sub_`/`data_` placeholders to their linker-proven US names is byte-neutral but barely moves the metric (calcprogress dedups labels into a set and the US-names already exist as counted `jp_syms` aliases); the ~1611 `banim_`/`gfx_` asset sheets are the hard ceiling. **D149 (broad small-fn vein, +10 — 7628→7638):** beyond efx, the still-asm pool has clean carveable singletons across the ROM: `bx lr` nullsubs the D100 sweep missed (nullsub_4/7/8/112/113 → `void f(void){}`; NOT `mov pc,lr` or explicit-trailing-`nop` ones); Proc_Find/Proc_Goto/Proc_Break wrappers (FadeOutCgText, GmapAutoMu_WaitInitialDelay); and the Proc_Find conditional-field-store class (ConfigSysHandCursorShadowEnabled/BmBgfxSetLoopEN/SetSysBrownBoxWidth) where the reliable fix is `int a; int aa=(s8)a;` at the top — the early cast forces the `asr` sign-ext held in a callee-saved reg across the call (a stored-only `s8`/`s16` PARAM otherwise zero-extends `lsr`), plus a pointer-base hoist (`u8 *q=(u8*)p+idx*8; q[OFF]=v`) to match the add operand order. DEAD/permuter: the StartBlocking-return-proc wrappers (JP copies the call result r0→r1 before field stores, agbcc won't), two-s16-param r4↔r5 reg-swap, and `&gSym+bigoff` literal-fold. **D148 (efx-init/Loop family, +13 in one iter — 7615→7628):** the still-asm `sub_<addr>` containing `gEfxBgSemaphore` (≈38) are efx BG/OBJ init & `_Loop` functions of a few shapes; many port to matching C directly or by replicating the fe8u source with JP ProcScr/data addresses swapped in (region-diff is usually just relocations or ONE extra call). Idioms cracked: `SetWinEnable(0,0,0)` = a trailing 3-AND DISPCNT-window bitfield clear on 0x03003020 (don't fold to `&0x1F`); the **data-symbol-binding** lever (`baseline_syms` line + `extern u8 g…[]` + `(int)g…`) defeats agbcc's constant-strength-reduction of two near-offset data pointers; **arg-eval-order** is controlled by hoisting the JP-first-evaluated arg into a temp (or inlining a late-materialized base address); a `(s16)` cast on an `int` param forces `asr` sign-ext (vs `lsr` for a stored-only `s16` param). DEAD/permuter-tier: lazy-sign-ext scheduling, computed-goto stack spills, s16-return sign-ext + proc reg-swap. **D147 (cont., +13 over two iters — moves BOTH axes):** the small unnamed asm `sub_` (4–28B) hand-decompile to matching C; each deletes an `asm/sub_*.s` placeholder so matching-C AND named both rise. Tooling that made it productive: (a) resolve each `bl sub_<addr>` callee (caller-local `.set`) to its real global name via `nm fireemblem8.elf` (sub_8002DE4=Proc_Break, sub_8019914=RenderBmMap, …) so callee-calling carves link; (b) write ROM-data pointers as absolute addresses `(void*)0x08763A5C` — they match the asm literal pool directly; (c) force `ldrsh` (vs agbcc's `ldrh` when a signed load only feeds a 16-bit store) with an `int` intermediate, and match base-pointer *scheduling* by declaring the dest pointer AFTER the signed load; (d) gate every carve on the full `fireemblem8.gba: OK` sha1 (never a range byte-diff — the stale-OK trap) and revert PER-FUNCTION (a broad `git checkout HEAD -- asm/ layout/` clobbers sibling carves). DEAD: arg-eval reg-alloc order (sub_80A4088), some memory-op scheduling (sub_800C3AC). **D147 (initial, +3):** the smallest still-asm `sub_<addr>` (4–24B, *unnamed*, so the D100 trivial-stub sweep skipped them) hand-decompile straight to matching C — `return 1`, store-arg-to-fixed-RAM, signed range-check. Carving each deletes an `asm/sub_*.s` placeholder, so matching-C AND named both rise (7529→7532; placeholders 2411→2408). Trap reconfirmed: carve+verify ONE at a time with a clean build — a link failure from a sibling carve (undefined non-global callee, e.g. a `sub_` only reachable via a caller-local `.set`) leaves `fireemblem8.gba` stale, so later per-carve byte-checks falsely report MATCH (the stale-OK trap); 4 batch siblings looked matched but a cold rebuild exposed real diffs (arg-eval reg-alloc, memory-op order). Callee-calling carves need the callee globally defined; reg-alloc-sensitive ones (arg order) stay DEAD. **D146 (shifted-domain sentinel, +6 — re-opened a class long-misfiled as a dead-end):** many region-different `sub_` differ ONLY in an `s16 == -1` / `!= -1` SENTINEL: JP compiles it shifted-domain (`lsls#16; cmp #0xFFFF0000`, reusing the value in r0), while US/agbcc (and fe8u's matching ROM, cross-checked) emits the asr form (`lsls#16; asrs#16; movs#1; negs; cmp #-1`) — 4 bytes longer, so every following branch target shifts and the whole-function byte-diff cascades from this ONE point. `autocarve` pre-screens these as `DEAD-END (sign-ext)` — WRONG for this class. The fix forces agbcc's shifted-domain compare with no reload/extra-local: a FIELD sentinel via assignment-in-condition `((proc->timer = ...duration) << 16) == 0xFFFF0000` (keeps proc in r4; an `int` local shifts reg-alloc r4→r5, `field << 16` adds a reload), an s16-returning-FUNCTION sentinel directly `(GetAnimNextRoundType(anim) << 16) != 0xFFFF0000`. Carved EfxSelfThunderBGMain + efx heal-staff Loop_Main family (efxRelive/Live/Reblow/Recover) + EfxDummymagicMain. Discriminator vs genuine reg-alloc/`lsr↔asr` dead-ends (EfxTmCpyExt 126/164 high-reg churn; UnitAutolevelRealistic lsr↔asr loop-counter): carve plain → disasm-diff → ONE asr-cluster + branch cascade = WIN, pervasive `mov sl/r8/r9` churn = DEAD. **D139 (autocarve-NEAR → IDA/diff, +3):** the autocarve harness's NEAR/COMPILE-fail flags feed per-function IDA: `GameIntroNintendoFadeIN` (JP drops an init block), `MusicOptionChangeHandler` (IDA: JP calls `Sound_FadeOutBGM(1)` not `Sound_StopBgmImmediate`), `SoundRoom_DrawCompletionPercent` (DECL_ONLY + JP draws % at `tm+6/+7` not `tm+8/+9` — a layout-coord const). The COMPILE-fail bucket is mostly DECL_ONLY (bound-but-undeclared sym) but the underlying fn is often region-different (reg-alloc: ClassIntroLetter_LoopFadeOut; whole-screen diff: DrawTitleSprites_Loop) — only the small-const/call-sub ones carve. **D138 (funclib sibling-swap fix, +3):** when a funclib-hinted carve byte-diffs in MANY constants, the hint may have SWAPPED two adjacent siblings — verify identity via IDA decompile (the JP constants) cross-checked against a distinguishing literal's address order (the Pal symbol order matching US). Fixed GameIntro{Nintendo,IntelligentSystems}FadeOUT (funclib swapped them); StartSpellAnimIvaldi (IDA: JP calls sub_8055E2C not NewEfxSpellCast). lsr↔asr param-store wrappers (Sio_StartBoxTransition*, NewEfxFlashHPBar) stay DEAD (agbcc collapses asrs→lsrs when a param's only use is a 16-bit store). **D135 (DECL_ONLY type-aware, +3):** `scripts/carve_declonly.py` carves a dedicated `asm/<Name>.s` fn that compile-fails ONLY because it references a symbol BOUND in the JP build (baseline_syms/cfbind) but UNDECLARED in JP headers — and autobind's generic `int X()` guess is the WRONG type. Extract the real US type → `extern struct Proc sProcArray[];`, recompile byte-perfect, full cold-compare gate. Carved Proc_FindNonBlocked/Proc_FindWithMark (TU-static gProc array sProcArray @0x02024E68) + Delete. (`scripts/decl_carve.py` only sweeps `sub_`; this handles dedicated named files.) Stranded-section carves remain a DEAD-END (removing a `stranded_<TU>.s` section shifts the whole ROM → 12.9M-byte diff). **D134 (non-`sub_` named + stranded pool, +10):** `scripts/screen_allnamed.py` (addr from `nm`, robust US regex) extends the screen to NAMED `.global` funcs in ANY `asm/*.s` — 682 candidates, 222 in non-`sub_` files, 78 CARVE. `autobind` was generalized (`recipe_fallback`) to carve dedicated `asm/<Name>.s` AND `stranded_<TU>.s` sections, with sentinel-bind for undeclared callees and a static-export fix (US-`static` fns made global so already-carved callers resolve). Carved a batch of core text/proc/bmap fns (ManimShiftingSineWaveScanlineBuf_Loop, ProcCmd_*, SpriteRefresher_OnIdle, WfxFlames_Init, ClearNativeCallback, PrintProcess*, EkrLvupHBlank, ResetEkrDragonStatus). Caveats: stranded-section carves can match the fn range yet break the full ROM (the cold-`make compare` gate is mandatory, D126); autobind needs a fresh `fireemblem8.elf` per candidate or `DEFINED` degrades → false UNMAPPED. **D133 (fast-screen CFAIL vein, +11):** a 60-second standalone screen (`scripts/screen_named_rd.py` + `screen_cfail.py` — compile the US body, diff only NON-reloc bytes, auto-declare undeclared data symbols and retry) triages the 357 NAMED region-different asm funcs into CARVE/NEAR/FAR/CFAIL, replacing a ~21-hour blind autobind sweep. The CFAIL bucket (compile-fails only on undeclared `ProcScr_*`/`Img_*`/`gPopup*` data syms) yielded 15 CARVE incl an 8-fn `StartSpellAnim*` cluster, each carved via `autobind` (binds the data syms to real baserom addrs); some asm tagged "region-different (D23)" was byte-perfect once a TU-local function-like macro (`#define RGB_(...)`, dropped by `extract_func_only`) is inline-expanded via `DECL_SUBST`. **D132 (aliased-function vein, re-opened the axis yet again, +12):** funcmap'd `sub_<addr>` asm functions that ALREADY carry a baseline US-name alias (linker-proven identity) but whose asm was still descriptive — MISSED by the FAR `nofuncmap_region_different` screen because they're not in it. A proper NEAR screen (extract US body → standalone agbcc compile → all byte-diffs fall at reloc offsets from `objdump -r`) found 11+1 carveable; each carved by porting the US body, binding any NEEDBIND data/proc symbol from the asm literal pool (reloc-off → fn_base+off → `.4byte`), dropping the baseline alias (the `.c` now provides the symbol), and gap-splitting the owning `frontier_df4_*` INCBIN for local-const `.rodata` (`AiFindBestAdjacentPositionByFunc`'s `adjacencyLut[8]` @ gap2a, `PutImg_PrepItemUseUnk`'s `Pals_PrepWindow[4]` ptr array @ gap21c). **D129 (region-same-uncarved vein, re-opened the axis again):** also handles local-const tables via the D121 gap-split — `Decompress`'s `static func_lut` (.data) and `TalkBubbleOpen_OnIdle`'s `imgList` (.rodata) placed by splitting the owning `frontier_df4_*` INCBIN (the table bytes were already region-same in the carved gap). The authoritative opportunity list = region-SAME funcs (`nofuncmap_region_same.tsv`, `tier==reloc-unique`) whose JP addr is NOT in any `src/*.o(.text)` range when the FULL manifest is unioned (`carved_rom.tsv` + `carved_rom.d/*.tsv` — synthetic-C `src/sub_<addr>.o` already counts; only `asm/*.o` descriptive-asm fns are the lever). ~35 such; harvested per-function with the DECL_ONLY-extern + RAM/data-bind path (read callee/table addrs from the asm literal pool, `extern T table[N]` to keep `ARRAY_COUNT` compiling). Trap: a reloc-unique fn that calls a trivial reloc-AMBIGUOUS callee (`SetPopupUnit`) whose bytes an `exact_<addr>.c` matched at the wrong addr → 1-byte BL diff; verify each `bl` target's nm-addr == the asm `.set` addr before carving. **D110**: a SHAPE-class sweep of the smallest still-asm `sub_` re-opened this axis again (+27 in one iteration) — a generalized register-dataflow **wrapper** carver (`[ret] f(args){ [return] CALLEE(args); }`, const into any arg register + pass-through params; void iff `pop {r0}` else int-return) plus the **str-to-field** / **cond-str** (`if(p) *(T*)(p+OFF)=a`) / **cond-return** (`return CALLEE(L)!=0`) classes, each carved ONE-AT-A-TIME (batch placement shifts the ROM) and COLD-`make compare`-gated. The rest are **gbadisasm descriptive asm** (disassembly, *not* decompilation) — the ongoing asm→C grind. (The decomp.dev **Code** badge above tracks the same progress by code *size* rather than function count — currently **~67%**, lower than the by-count figure because the functions still in asm are larger on average; descriptive asm counts as *unmatched* there, as in fe8u/decomp.dev.) NB: the honest ceiling is **8,209**, not 8,528 — ~170 functions (ARM-mode `arm.o`/`m4a_1`, libgcc/libc) are hand-assembly even in the US decomp; against that ceiling matching-C is **~84.2%**. **D102**: with the mechanical levers swept (D100), the live frontier is per-function hand-decomp, now with two complementary tools — (a) `scripts/graduate_jp_batch.py`, an efficient batch harvester (wire-all → build-once → byte-check → revert-mismatches) that graduates non-leaf jp-aliased straight-porters (bl/literal relocs resolve at the JP link); (b) **manual hand-decomp** for what the batch can't see — region-different *logic* (e.g. `GetStringLineEnd`'s JP variable-width text encoding), US-inline functions (`extract_func_only` finds no clean def — re-derive from asm + struct headers, providing inlined accessors like `GetUnitMaxHp`/`GetUnitPower` as `extern inline`), and codegen-shape matches (if/else vs ternary, result-temp vs early-return). **D100**: the deterministic **stub-graduation** tier is now swept — `scripts/carve_trivial_funcs.py` recompiles trivial region-different gbadisasm stubs the disasm pass never *tried* as C (`bx lr`→`void f(void){}`, `movs r0,#N`→`return N`, `ld*/st* [r0,#N]` accessors, const/zero field-stores, semaphore inc/dec) to byte-identical agbcc output, make-compare-gated (+114 funcs). Both the mechanical matching-C levers and the reliable funcmap-naming lever are now exhausted (verified: 0 still-asm placeholders carry a funcmap name — they are placeholders *because* they are region-different/unmatched); what remains is the genuine per-function hand-decomp/permuter frontier. Key (D60/D63/D64/D67/D68/D81/D89): most of the remaining was mechanically reachable — the "~2,224 region-different" estimate was largely a classifier false-negative. Levers that cleared the bulk: the fast **reloc-resolve carve** (`scripts/perm2_graduate.py` — region-SAME modulo relocation), **CF:agbcc data-binding** (bind the TU-private data table a function references, then it compiles byte-exact), **per-frag region-same harvest** (`scripts/perfrag_carve.py`), the **constant-diff carve** (`scripts/const_diff_carve.py` — D81: most of the "FAR" tail differs from US bytes ONLY in JP data-constants like msg-IDs `0x8A3`→`0x843`; substitute the JP literal and it compiles byte-exact), and the **codegen-shape type-widening sweep** (D89: the true-codegen FAR/LEN tail is dominated by a mechanizable temp-var-width delta — JP declares a local `int` where US uses `s8`/`s16`/`bool`, so agbcc holds one int temp instead of re-narrowing; sweep `signed/bool → int` per declaration + verify). The remaining hand-decomp/permuter frontier (reg-allocation/temp-ordering + the `lsr↔asr` compiler-extension residue + data-table-blocked + pure-JP) is materially smaller. **D108 (coddog-on-backlog, the breakthrough that re-opened this axis after it looked exhausted):** the un-carved `sub_<addr>` functions were invisible to coddog (size-0 ELF symbols); `scripts/tools/coddog/size_backlog.py` adds byte-neutral `.size` so `coddog compare2` can match them against the US ELF by opcode stream — surfacing **413 sub_ at 99.99%/100%** (region-SAME modulo relocation) that the funcmap never identified. Each is carved by renaming `sub_<addr>`→its US name + `autobind.run` (binds the data/proc symbols `graduate_jp_batch` misses); the const-different ones decode per-function (JP msg-IDs, JP-variant calls). +matching-C AND +named together. Candidate list: `reference/coddog/region_same_candidates.txt`. The carve is driven by a **self-committing multi-wave harvest** (`~/cod_harvest.sh` + `cod_waves.sh`): each pass skips already-carved, renames+`autobind.run` per candidate, double-`make compare`-gates, and commits each match individually — so a carved function entering the link unlocks its un-carved *callers* on the next pass (dependency cascade; e.g. `AiDoBerserkMove`→`AiScriptCmd_05/08`→`AiFunc_AttackUnitWithCharId`). Two harness bugs were root-caused and fixed: (a) stale `fireemblem8.elf`/`.gba` between candidates poisoned the next build → false no-matches; (b) `autobind`'s `DEFINED=defined_elf()` read a missing/stale elf → it re-bound already-aliased symbols (`BG_Fill`, `UpdateMenuItemPanel`) → duplicate `.set` → false `[LINK]`. Fix: `clean()` restores a cached HEAD-state elf each candidate (correct DEFINED, ~2× faster). | (region-same-uncarved vein, re-opened the axis again):** also handles local-const tables via the D121 gap-split — `Decompress`'s `static func_lut` (.data) and `TalkBubbleOpen_OnIdle`'s `imgList` (.rodata) placed by splitting the owning `frontier_df4_*` INCBIN (the table bytes were already region-same in the carved gap). The authoritative opportunity list = region-SAME funcs (`nofuncmap_region_same.tsv`, `tier==reloc-unique`) whose JP addr is NOT in any `src/*.o(.text)` range when the FULL manifest is unioned (`carved_rom.tsv` + `carved_rom.d/*.tsv` — synthetic-C `src/sub_<addr>.o` already counts; only `asm/*.o` descriptive-asm fns are the lever). ~35 such; harvested per-function with the DECL_ONLY-extern + RAM/data-bind path (read callee/table addrs from the asm literal pool, `extern T table[N]` to keep `ARRAY_COUNT` compiling). Trap: a reloc-unique fn that calls a trivial reloc-AMBIGUOUS callee (`SetPopupUnit`) whose bytes an `exact_<addr>.c` matched at the wrong addr → 1-byte BL diff; verify each `bl` target's nm-addr == the asm `.set` addr before carving. **D110**: a SHAPE-class sweep of the smallest still-asm `sub_` re-opened this axis again (+27 in one iteration) — a generalized register-dataflow **wrapper** carver (`[ret] f(args){ [return] CALLEE(args); }`, const into any arg register + pass-through params; void iff `pop {r0}` else int-return) plus the **str-to-field** / **cond-str** (`if(p) *(T*)(p+OFF)=a`) / **cond-return** (`return CALLEE(L)!=0`) classes, each carved ONE-AT-A-TIME (batch placement shifts the ROM) and COLD-`make compare`-gated. The rest are **gbadisasm descriptive asm** (disassembly, *not* decompilation) — the ongoing asm→C grind. (The decomp.dev **Code** badge above tracks the same progress by code *size* rather than function count — currently **~67%**, lower than the by-count figure because the functions still in asm are larger on average; descriptive asm counts as *unmatched* there, as in fe8u/decomp.dev.) NB: the honest ceiling is **8,209**, not 8,528 — ~170 functions (ARM-mode `arm.o`/`m4a_1`, libgcc/libc) are hand-assembly even in the US decomp; against that ceiling matching-C is **~84.2%**. **D102**: with the mechanical levers swept (D100), the live frontier is per-function hand-decomp, now with two complementary tools — (a) `scripts/graduate_jp_batch.py`, an efficient batch harvester (wire-all → build-once → byte-check → revert-mismatches) that graduates non-leaf jp-aliased straight-porters (bl/literal relocs resolve at the JP link); (b) **manual hand-decomp** for what the batch can't see — region-different *logic* (e.g. `GetStringLineEnd`'s JP variable-width text encoding), US-inline functions (`extract_func_only` finds no clean def — re-derive from asm + struct headers, providing inlined accessors like `GetUnitMaxHp`/`GetUnitPower` as `extern inline`), and codegen-shape matches (if/else vs ternary, result-temp vs early-return). **D100**: the deterministic **stub-graduation** tier is now swept — `scripts/carve_trivial_funcs.py` recompiles trivial region-different gbadisasm stubs the disasm pass never *tried* as C (`bx lr`→`void f(void){}`, `movs r0,#N`→`return N`, `ld*/st* [r0,#N]` accessors, const/zero field-stores, semaphore inc/dec) to byte-identical agbcc output, make-compare-gated (+114 funcs). Both the mechanical matching-C levers and the reliable funcmap-naming lever are now exhausted (verified: 0 still-asm placeholders carry a funcmap name — they are placeholders *because* they are region-different/unmatched); what remains is the genuine per-function hand-decomp/permuter frontier. Key (D60/D63/D64/D67/D68/D81/D89): most of the remaining was mechanically reachable — the "~2,224 region-different" estimate was largely a classifier false-negative. Levers that cleared the bulk: the fast **reloc-resolve carve** (`scripts/perm2_graduate.py` — region-SAME modulo relocation), **CF:agbcc data-binding** (bind the TU-private data table a function references, then it compiles byte-exact), **per-frag region-same harvest** (`scripts/perfrag_carve.py`), the **constant-diff carve** (`scripts/const_diff_carve.py` — D81: most of the "FAR" tail differs from US bytes ONLY in JP data-constants like msg-IDs `0x8A3`→`0x843`; substitute the JP literal and it compiles byte-exact), and the **codegen-shape type-widening sweep** (D89: the true-codegen FAR/LEN tail is dominated by a mechanizable temp-var-width delta — JP declares a local `int` where US uses `s8`/`s16`/`bool`, so agbcc holds one int temp instead of re-narrowing; sweep `signed/bool → int` per declaration + verify). The remaining hand-decomp/permuter frontier (reg-allocation/temp-ordering + the `lsr↔asr` compiler-extension residue + data-table-blocked + pure-JP) is materially smaller. **D108 (coddog-on-backlog, the breakthrough that re-opened this axis after it looked exhausted):** the un-carved `sub_<addr>` functions were invisible to coddog (size-0 ELF symbols); `scripts/tools/coddog/size_backlog.py` adds byte-neutral `.size` so `coddog compare2` can match them against the US ELF by opcode stream — surfacing **413 sub_ at 99.99%/100%** (region-SAME modulo relocation) that the funcmap never identified. Each is carved by renaming `sub_<addr>`→its US name + `autobind.run` (binds the data/proc symbols `graduate_jp_batch` misses); the const-different ones decode per-function (JP msg-IDs, JP-variant calls). +matching-C AND +named together. Candidate list: `reference/coddog/region_same_candidates.txt`. The carve is driven by a **self-committing multi-wave harvest** (`~/cod_harvest.sh` + `cod_waves.sh`): each pass skips already-carved, renames+`autobind.run` per candidate, double-`make compare`-gates, and commits each match individually — so a carved function entering the link unlocks its un-carved *callers* on the next pass (dependency cascade; e.g. `AiDoBerserkMove`→`AiScriptCmd_05/08`→`AiFunc_AttackUnitWithCharId`). Two harness bugs were root-caused and fixed: (a) stale `fireemblem8.elf`/`.gba` between candidates poisoned the next build → false no-matches; (b) `autobind`'s `DEFINED=defined_elf()` read a missing/stale elf → it re-bound already-aliased symbols (`BG_Fill`, `UpdateMenuItemPanel`) → duplicate `.set` → false `[LINK]`. Fix: `clean()` restores a cached HEAD-state elf each candidate (correct DEFINED, ~2× faster). |
| **Extracted data** | **100.00%** ✅ (13,938,060 / 13,938,060 — **true 100%, 0 B residual**) | genuinely-extracted asset bytes (typed C struct tables / PNG) ÷ data bytes. Named `.incbin "baserom.gba"` (of a committed `.bin`) is **not** counted as extraction. The big jump (D71) emits the ~404 KB JP message block as **compiled C** (`src/msg_data.c`: Huffman bitstreams + tree + `gMsgTable`, US-shaped) instead of a `.byte` asm blob — byte-identical after link; plus typed game-data tables (chapter/affinity/item/character/class/AI, D70–D76). **D97**: the deferred **graphics/gbagfx pipeline is proven portable** — JP ROM bytes round-trip through PNG→`gbagfx`→`INCBIN_U8` typed C byte-for-byte (the `DATA_INCBIN_CFILES` preproc path). **D101**: the original "~50-70% ceiling" estimate was **wrong** — `scripts/asmgfx2c.py` was hardened to handle multi-`.incbin` symbols (preproc `INCBIN_U8("a","b",..)` concat), genuine slices (`INCBIN_U8("X",off,len)`), the `extern u16 CONST_DATA SYM[]` type form, and the agbcc-accepted alias form (`extern T X[1] __attribute__((alias(Y)))`), with a `baserom`-incbin guard so nothing passes WARM but fails COLD — driving data **3.51% → 95% → 99.996%**, all COLD-`make compare`-verified. **D125 closed the axis to TRUE 100%** (0 B residual): the final 546 B — `p05.27` (mprec), `blanks.12`/`zeroes.13` (vfprintf), and `chance_lut.3` (worldmap) — were the last `data_asm` bytes, blocked only because their compiler-generated **static-local labels contain a dot** (`name.N`), which is not a legal C identifier. Solved with the **GNU C `asm()` symbol-alias** trick: `u8 chance_lut_3[] asm("chance_lut.3") = INCBIN_U8(...)` — a legal C variable that the linker emits under the exact dotted symbol name (verified: `.globl chance_lut.3` in the preproc→agbcc output), so all references still resolve and no rename/dual-def risk. All three migrated to `src/data/{libc,worldmap_gmapunit}/*.c`, COLD-`make compare`-verified. The earlier "firm ceiling / libc not game source" framing was wrong — region-same committed `.bin` data *is* extractable to typed source regardless of origin. D103/D104 cleared the reachable tier: header-typed plain `u8`/`u16` tables (`gPromoJidLut[][2]`, `gAnimCharaPalConfig[0x100][7]`, `gUnkData_3x`, Statscreen/HelpTextBox gfx) via type-matched `INCBIN_U8/U16`; the 3 `TextGlyphs_{System,Talk,Special}` `struct Glyph *[]` pointer tables and the `lut.29`/`bldyLut.10` dotted labels via INCBIN raw bytes + the agbcc-accepted typed alias (`extern T X[1] __attribute__((alias(Y)))`) under the descriptive name the consumers already use. |
| **Named symbols** | **~85.6%** (13,649 / 15,953) | labels with meaningful names ÷ total labels. The rest are `sub_/data_/nullsub_/sheet` placeholders (the `banim_`/`gfx_` asset sheets, ~1,687, are the practical ceiling → max ≈ 89.6%). **D114** is the biggest post-funcmap lever — **naming is DECOUPLED from byte-matching**: a region-different `sub_<addr>`/`data_<addr>` that can never byte-match (lsr↔asr, data-pointer-reorder, struct-size, codegen-shape dead-ends) still has a linker-PROVEN US identity whenever a carved caller references it by US name and the ROM links byte-perfect — that proof shows up as a `baseline_syms` alias at the address. Rename the placeholder → US name byte-neutrally (section-preserving `sed`; `make compare` re-gates). Confirmation tiers: (1) baseline alias = linker proof; (2) mnemonic-exact (US-compile objdump == JP asm mnemonics); (3) callee-overlap (≥2 resolved BL callees ⊆ the funclib-hinted US function's callee set — funclib positional hint + behavioral signal); (4) DATA alias; (5) mnemonic ≥90% + len≥8. Swept ~520 placeholders (231 alias + 113 data-alias + 105 callee-overlap + mnemonic batches), named 80.7%→83.3%. The funcmap-naming lever (byte-verified) is exhausted, and **D106** adds a *reliable* lever for the region-different backlog: **callee-fingerprint identification** (`scripts/fingerprint_identify.py`). A region-different `sub_<hex>` can't be byte-matched, but its set of resolved real-name BL callees is a behavioral fingerprint; when exactly ONE unmapped US function's callee-set is a superset of it (≥3 callees), that's a unique identification, then **cross-validated** by (a) callee-set Jaccard ≥0.7 and (b) positional-delta consistency with neighbouring funcmap anchors — two independent signals. Renaming is byte-neutral (`make compare` re-gates integrity). This caught that 7/9 positional-bracket (`bracket_identify.py`) names were actually *wrong* (NOMATCH/WEAK fingerprint), so positional evidence alone is **not** trusted. First two batches: **+43** (30 GOLD at Jaccard ≥0.7, then 13 SILVER at ≥0.6) — all double-confirmed; the harness skips any name already defined by a carved C function or bound in `baseline_syms` (a multiple-definition link error is the final gate), so e.g. `efxDarkGradoOBJ01piece_Loop` (already a C function) was correctly excluded. |

Progress is tracked on the [project board](https://github.com/users/laqieer/projects/3)
and charted live on the [**FE Decomp Portal**](https://laqieer.github.io/fe-decomp-portal/)
(alongside fe8u / fe6 / fe7j). See [`docs/strategy.md`](docs/strategy.md) for the
porting methodology.

## Building

You need the ARM toolchain (`binutils-arm-none-eabi`) and, for C decompilation,
`agbcc` installed into `tools/agbcc` (same as the US decomp).

```bash
# Place your own copy of the original ROM here:
#   ./baserom.gba   (sha1 7da0456035366aa18414faa79d8fe7649f03c1ed)
make compare       # builds fireemblem8.gba and verifies the sha1
```

Success ends with:
```
fireemblem8.gba: OK
```

**`baserom.gba` is NOT a build input** — every ROM byte comes from committed source
(matching C, descriptive asm, extracted assets, committed `data/*.bin`). It is used
*only* to verify the result. The **self-contained build** (the achieved end state):

```bash
mv baserom.gba /tmp/ && make           # ✅ builds the byte-identical ROM from source ALONE
mv /tmp/baserom.gba . && make compare   # restore ONLY to verify: sha1 -> OK
```

Check the current self-containment with `python3 scripts/check_selfcontained.py`.

## Layout

| Path             | Purpose                                                        |
|------------------|----------------------------------------------------------------|
| `baserom.gba`    | Original JP ROM (you provide; gitignored).                     |
| `asm/*.s`        | Carved/descriptive assembly + data. **~83% is still `.incbin "baserom.gba"`** (generated `asm/baserom.s` + per-region `.s` files) — replaced by extracted source as decompilation progresses. |
| `src/`           | Decompiled C (ported/adapted from the US decomp).              |
| `include/`       | Headers (ported from the US decomp).                           |
| `ldscript.txt`   | ROM layout; decompiled objects are placed ahead of the incbin. |
| `tools/agbcc`    | The GCC 2.95 ARM compiler (install locally; gitignored).       |
| `docs/`          | Strategy & methodology notes.                                  |

## Reverse engineering

For the remaining region-different functions (text/font/menu/save) that must be
hand-decompiled against the JP ROM, two decompilers are wired up **headlessly
over MCP** so Claude Code (and the autonomous loop) can ask for pseudo-C of a JP
address: **IDA Pro Hex-Rays** (server `ida`, the workhorse — `make ida-db`) and
**Ghidra** (server `ghidra`, an open-source cross-check — `make ghidra-db`).
Both are fed the project's own `fireemblem8.elf`. To close the last gap when a
ported function compiles *close* but not byte-exact, **decomp-permuter** (upstream,
which now supports ARM32/Thumb + agbcc) is wired up via `scripts/permuter/`. Setup,
rationale and usage are in [`docs/reverse-engineering.md`](docs/reverse-engineering.md)
and [`docs/decisions.md`](docs/decisions.md) (D6). The IDA/Ghidra/permuter installs
and databases are local/gitignored; only the scripts/config are tracked.

## License

Decompilation infrastructure mirrors the US decomp. Game assets/data are the
property of Nintendo / Intelligent Systems; no copyrighted ROM data is committed.
