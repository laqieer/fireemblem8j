# FE8J decomp frontier

> **This is the single source of truth for current work.** Historical carve
> evidence belongs in `docs/decisions.md`; generated binary-form detail belongs
> in `docs/bin_audit.md`. Do not derive a work list from
> `layout/nofuncmap_*.tsv`.

## Canonical snapshot

Recomputed on 2026-07-15 from
`main@9a291a0cf4e6d748f273e14f8bbd064178de9056` after `make layout` and
`make compare` (`fireemblem8.gba: OK`). Refresh this section by running the
commands, not by copying its numbers:

```sh
python3 scripts/calcprogress.py
python3 scripts/audit_bin_forms.py
python3 scripts/check_selfcontained.py
make shiftcheck
python3 scripts/check_incbin_deps.py
python3 scripts/check_layout.py
```

### Four-axis progress

| Axis | Current canonical result | Target |
|---|---:|---:|
| Build self-containment | **100.00%** (16,777,216 / 16,777,216; 0 baserom incbins) | 100% |
| Matching-C functions | **99.97%** (8,689 / 8,692; 8,562 compiled C + 127 library; 3 descriptive asm) | 100% |
| Extracted data, source-form | **100.00%** (13,937,336 / 13,937,336) | 100% |
| Named symbols | **100.00%** (2,599 / 2,599; 0 placeholders) | 100% |

Supplemental data metric: the strict C/PNG-under-`src/` subset is **79.91%**
(11,137,425 / 13,937,336). It intentionally excludes valid banim and sound
source forms and is not the extracted-data gate.

Source-form 100% is an **allow-listed object-root metric**, not a zero-binary
claim. It credits approved source-producing roots while accepting their
legitimate binary inputs, including the 1,452 evidence-backed FLOOR files.
Therefore it means neither zero `.bin` files, zero FLOOR inventory, nor that
every credited byte is strict typed/structured source.

Named 2,599/2,599 is specifically the current literal `.global`
assembly-label census performed by `calcprogress.py`. It is not all ELF/C
symbols and cannot be compared directly with historical 12,688-style
denominators. Run the script only after the project toolchain/library build is
installed and populated, especially `tools/agbcc/lib`; otherwise its 127
linked library functions disappear and the function line can misleadingly
report 8,562/8,565 rather than 8,689/8,692.

### Current issue-143 and shiftability evidence

- Binary-form audit: **MISS=0, FLOOR=1452, UNCERTAIN=0, TOTAL=1452**.
  `docs/bin_audit.md` is generated by `scripts/audit_bin_forms.py`.
- `UNCERTAIN=0` means the concrete, tracked binary-form **actionable residual
  list is empty**. It does **not** mean complete decompilation, zero opaque
  assets, or zero binary-form debt: **1,452 evidence-backed FLOOR entries
  remain**.
- Pointer audit: **59,524** ROM `R_ARM_ABS32` relocations; 5,678 function
  targets (5,672 Thumb + 6 even); 0 function interiors; 0 header-domain,
  large-addend, compressed-anchor, AREA-scalar, or placement-anchor failures.
- Opaque self-reference evidence: 8 resolved symbols / 267 hit words,
  0 unresolved, completion gate 0 **within the exact-extent population**.
  The explicit blind spot remains: **1,075 zero-size opaque symbols have no
  proven extent and are skipped, including 865 `AnimSprite_*` labels**.
- Glyph relocation coverage:
  `TextGlyphs_System` **1488/1488 relocated**, `TextGlyphs_Talk`
  **2085/2085 relocated**, 0 missing/malformed. The typed
  `SjisGlyphs_0859140C` run covers 152 records with exactly 152 inbound links
  (**109 external + 43 internal**), all relocated.
- ProcCmd relocation coverage: 995 prefixes / 7,779 records;
  **4,706/4,706 unique non-null pointer slots relocated**, 0 missing/malformed,
  and 13 deduplicated pointer fields correctly NULL. Eight Proc-like zero-size
  names remain explicitly unrecovered and are not claimed as decoded scripts.
- Code-literal gate: 6,209 linked code C sources scanned, 263 packed-value
  tokens classified, 0 unexplained raw ROM literals.
- Graphics gate: **1,062/1,062** map binaries valid; 2,346
  `AnimSprite_*`/`AnimScr_*` labels covered (1,461 explicit + 885 derived);
  **5,444/5,444** linked graphics-payload INCBIN names, 0 unlinked and
  0 relocation-slot overlap violations.
- Layout/dependencies: 10,487 ROM sections, 0 baseline gaps, 246 RAM sections,
  2,737 baseline symbols, 100.0000% ROM coverage; all 8,221
  layout-referenced objects and generated INCBIN dependencies validate.

The blocking `make shiftcheck` gate covers Layers 0/1/1b and the talk,
pointer-classification, code-literal, opaque-self-reference, graphics,
glyph, ProcCmd, and focused-test gates. Packed-layout differential Layer 2
remains non-gating/not applicable under D313.

## Active work

### Matching-C frontier

Ground truth is the three tracked `src/nonmatching/*.c` files:

1. `sub_800A34C.c`
2. `sub_800A594.c`
3. `sub_807D3BC.c`

Use `docs/nonmatching.md` for their current scratch/provenance details.
`make compare` is the only byte-match oracle. Treat compiler-wall labels as
working evidence, not permanent proofs.

### Binary-form frontier

There are no active `MISS` or `UNCERTAIN` filenames to dispatch. The completed
ending000/ending016, System-SJIS run, player-rank, StaffReel 6/7, and
ending-CG-tail providers are historical D397-D399 evidence, not active targets.
The 24-byte ending-CG partial-tile fragment remains a pinned FLOOR and must not
be relisted as actionable.

### Issue #143 status

This snapshot supports a **candidate documentation handoff only**:

- the binary-form actionable list is zero;
- all current public gates above pass;
- 1,452 FLOOR entries and the zero-size-symbol/`AnimSprite_*` extent blind spot
  are explicitly retained;
- D400 records only the immutable-trailer metadata waiver; no final closure
  verdict has yet been issued.

An independent verifier and the sole integrator decide whether the evidence is
sufficient to close issue #143. Do not label the issue final-MATCH from this
document.

## Frontier maintenance rules

1. Recompute every numerator and denominator from canonical commands whenever
   an axis or gate moves.
2. Keep only current target lists here; append historical rationale to
   `docs/decisions.md`.
3. Never treat `MISS=0`, `UNCERTAIN=0`, byte-perfect output, or gate 0 as proof
   of zero opaque/FLOOR debt outside the gate's stated scope.
4. A code target is real only if it remains in `src/nonmatching/` or otherwise
   still links from descriptive asm. Spot-check the tree before dispatch.
5. Never use `layout/nofuncmap_*.tsv` as the frontier; it is a stale
   classification cache, not a current work list.
