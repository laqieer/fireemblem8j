# validated reference-C inputs

Place only mechanically generated reference C here, named `<fn>_ref.c`, after it
has been validated against `asm/<fn>.s` by an independent ASM-vs-ASM proof.
Unvalidated m2c/US-C output must not be used for a `PROVEN` CBMC verdict.
