#include "global.h"

/* Migrated from asm/dat___malloc_av__ref.s (region-same graphics, single section).
 * Each symbol kept in the original section/order; byte-identical via INCBIN_U*.
 */

SECTION(".rodata.dat___malloc_av__ref") u8 __malloc_av_[] = INCBIN_U8("data/residual/__malloc_av_.bin");
