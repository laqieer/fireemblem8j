#include "global.h"

/* Migrated from asm/dat_anim_pike_trap.s (region-same graphics, single section).
 * Each symbol kept in the original section/order; byte-identical via INCBIN_U*.
 */

SECTION(".rodata.dat_anim_pike_trap") u8 SpriteAnim_PikeTrap[] = INCBIN_U8("data/residual/SpriteAnim_PikeTrap.bin");
