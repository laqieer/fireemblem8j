#include "global.h"

/* Migrated from asm/data_081CA04C.s (region-same graphics, single section).
 * Each symbol kept in the original section/order; byte-identical via INCBIN_U*.
 */

u8 data_081CA04C[] __attribute__((section(".data.residue.081CA04C"))) = INCBIN_U8("data/residual/data_081CA04C.bin");
