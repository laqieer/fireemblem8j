#include "global.h"

/* Migrated from asm/data_086B8608.s (region-same graphics, single section).
 * Each symbol kept in the original section/order; byte-identical via INCBIN_U*.
 */

/* D312 ID: byte-identical (96 B, whole symbol; unique single occurrence in the
 * fe8u ROM) to fe8u AnimSprite_EfxSleepOBJ2_107. Byte-neutral rename; section unchanged => same ROM bytes. */
u8 AnimSprite_EfxSleepOBJ2_107[] __attribute__((section(".data.residue.086B8608"))) = INCBIN_U8("data/residual/data_086B8608.bin");
