#include "global.h"

/* Migrated from asm/dat_gSoundRoomTable_ref.s (region-same graphics, single section).
 * Each symbol kept in the original section/order; byte-identical via INCBIN_U*.
 */

SECTION(".rodata.dat_gSoundRoomTable_ref") u8 gSoundRoomTable[] = INCBIN_U8("data/sound/gSoundRoomTable.bin");
