#include "global.h"

/* Migrated from asm/dat_gWMPathData_ref.s (region-same graphics, single section).
 * Each symbol kept in the original section/order; byte-identical via INCBIN_U*.
 */

SECTION(".rodata.dat_gWMPathData_ref") u8 gWMPathData[] = INCBIN_U8("data/residual/gWMPathData.bin");
