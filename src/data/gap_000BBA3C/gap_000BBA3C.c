#include "global.h"

/* Migrated from asm/gap_000BBA3C.s (region-same graphics, single section).
 * Each symbol kept in the original section/order; byte-identical via INCBIN_U*.
 */

u8 gap_000BBA3C[] __attribute__((section(".rodata.gap_000BBA3C"))) = INCBIN_U8("data/residual/gap_000BBA3C.bin");
